#
# Defines environment variables.
#
#   define all environment variables here
#

# Ensure that a non-login, non-interactive shell has a defined environment.
if [[ ( "$SHLVL" -eq 1 && ! -o LOGIN ) && -s "${ZDOTDIR:-$HOME}/.zprofile" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprofile"
fi

# Source other env variables
if [[ -s "${ZDOTDIR:-$HOME}/.zsecrets" ]]; then
  source "${ZDOTDIR:-$HOME}/.zsecrets"
fi


# ENV VARIABLES
# Add folders/locations to $PATH
export PATH="$HOME/bin:$PATH";
export PATH="$PATH:/usr/local/sbin";
hash cargo && export PATH="$PATH:$HOME/.cargo/bin"
hash code && export PATH="$PATH:/Applications/Visual Studio Code.app/Contents/Resources/app/bin"
[ -d '/usr/local/opt/binutils/bin' ] && export PATH="/usr/local/opt/binutils/bin:$PATH"
[ -d '/usr/local/lib/ruby/gems/2.6.0/bin' ] && export PATH="/usr/local/lib/ruby/gems/2.6.0/bin:$PATH"
[ -d '/usr/local/opt/ruby/bin' ] && export PATH="/usr/local/opt/ruby/bin:$PATH"
[ -d "$HOME/.chefdk/gem/ruby/2.6.0/bin" ] && export PATH="/Users/jniec/.chefdk/gem/ruby/2.6.0/bin:$PATH"

# Set Github API token for Homebrew
# set in ~/.zsecrets

# Go path settings
hash go && export GOPATH="~/.go"
hash go && export GOBIN="~/.go/bin"

# Ruby pkgconfig path
[ -d '/usr/local/opt/ruby/lib/pkgconfig' ] && export PKG_CONFIG_PATH="/usr/local/opt/ruby/lib/pkgconfig"

# Java Classpath
[ -d '/Library/Java/Extensions' ] && export CLASSPATH="$CLASSPATH:/Library/Java/Extensions/TwsApi.jar"

# Gpg TTY
GPG_TTY=$(tty)
export GPG_TTY

ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE="fg=245,faint"
